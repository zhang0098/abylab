**中文** · [English](CHANGELOG.en.md)

# 更新日志

这个文件记录 abylab 每一次发布里值得用户知道的变更。格式参考
[Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循
[语义化版本](https://semver.org/lang/zh-CN/)。
内部细节（工作区指令、上下文压缩、会话持久化、目标轮次、卸载）写在
[TECH.md](TECH.md)；日常用法看 [README.md](README.md)。

## [Unreleased]

## [0.1.17] - 2026-09-25

### 修复

- 剪贴板命令提前退出、关闭输入管道时，不再因 SIGPIPE 终止整个 abylab 和运行中的任务；
  复制失败会继续尝试备用方式。

## [0.1.16] - 2026-09-25

### 变更

- 思考结束后不再占一行。以前每段思考收尾都留一行 `✻ 思考 · 9.1s · 91 行`：它只说了这段思考什么时候
  发生、跑了多久、有多少行，读的时候一个字都用不上，而一轮里思考十几次就是十几行高度。现在这一行
  连同它上面的空行一起消失，思考块在摘要与预览下彻底不占地方。要看思考正文照旧 `ctrl+o` 切到全展开：
  那时每段思考顶上留一行 `✻ 思考` 当标题（秒数与行数不再回来——正文摊开后行数就是它自己）。还在跑的
  那句照旧显示，`✻ 思考中… 3s`（英文界面 `✻ thinking… 3s`）：它此前不管界面语言一律写英文，现在
  跟着界面语言走。
- 项目自我介绍换了口径。README 与 abylab.ai 首页此前写「极简主义的 DeepSeek harness，基于 Rust、
  ratatui 和 abycore DeepSeek SDK」，说的都是它由什么拼成；现在说的是拿它能干什么——「开源免费的
  极简主义 AI 助理：你可以用它来编程、修改 Excel 文件、写报告、调整电脑设置，只需要给它配上
  DeepSeek API Key 就可以开始干活」。站点的标题、meta 描述、分享卡片（`og:title`）以及 README
  banner 的 alt 与 SVG 描述一并跟上；`abycore`、`ratatui` 这些实现细节从门面退到 [TECH.md](TECH.md)
  （那里一直写着），README 末尾「参考了 DeepSeek Harness」的致谢照旧。

## [0.1.15] - 2026-09-24

### 新增

- 输入框边框左上角常驻一个 `abylab.ai`（站点地址），像窗口标题那样。空着的那一格以前什么都不写，
  现在归品牌；有动作提示（tip）或待办清单时它们优先占用这一行，提示过去了品牌回来。
- `/clip`（以及 ctrl+v、`@` 选中的图片）暂存后预览卡自动弹出来，并一直留在屏幕上。此前预览卡
  只在鼠标悬停图片、或光标恰好在图片上/紧挨着时出现：`/clip` 之后你一打字，光标离开图片，
  卡就没了——想边看图边写说明做不到。现在新暂存的图片被「钉住」，卡片跟着这张图存在
  （⌫ 删掉图片、esc 清空草稿、发送出去都会一起收掉），期间把鼠标移到别的图片上仍然看那一张。
  `/help` 里 `/clip` 那一行也写明了这一点。

### 修复

- 空草稿时光标块压在占位提示的首字上：`描述你想构建的内容…` 的「描」被框成一个方块。现在提示字
  从光标右一格开始画，光标独占一格空白（反色出来是实心块），跟草稿里有字时的光标长得一样。
- ask_user_question 弹窗选中「其他」时，光标留在 composer 里。弹窗开着的时候键盘整个归弹窗
  （所有按键都先给弹窗，输入框一个也拿不到），但输入框的光标块照旧画在自己的草稿上，
  连 IME 候选框也锚在那边——打给弹窗的字看起来像打进了输入框。现在只要有弹窗占着键盘，
  composer 就不画光标；提问弹窗把光标画在自己的「其他：」字段里：选中那一行时它紧跟在标签
  后面（字段空着就只显示光标，那行不再显示「输入回答」这类提示字），并跟着你打的字往后走；
  权限弹窗压在提问上时两边都不画光标。

### 变更

- 中断本轮改成连按两次 `esc`。以前运行中按一次 `esc` 立刻取消，误触一次就丢掉整轮；现在第一次
  只「上膛」——tip 提示「再按一次 esc 中断本轮」，1.5 秒内有效（跟 ctrl+c 那个退出节拍同一套），
  第二次才真发中断。两次都不会动草稿；关弹窗、取消排队编辑、收起建议这些单击动作照旧。
  `/help`、`/keys` 和 README 的 esc 说明同步改。
- 输入框上沿右端的 `↥` 前面补了一个圆点（跟工作区路径那个同一淡色调），把路径和箭头分开：
  `· ~/project/abylab:main · ↥`。
- 安装命令换成 `curl -fsSL https://abylab.ai/install.sh | bash`。脚本还是同一份（还是 curl 那个
  地址），只是不再套一层 `/bin/bash -c "$(…)"`。README 中英、站点首页、`install.sh --help`
  和 `site/_redirects` 里的注释一起改。脚本从不读标准输入（PATH 那段的 `read` 读的是这里的
  heredoc），所以管道形式不会吞掉脚本内容。
- 去掉了输入框上沿右端的 `⛶` 放大按钮。它只能靠鼠标点，点一下把输入框钉到 5/8 屏高、再点一下还原；
  现在输入框高度只跟着草稿行数自动增长（半屏、最多 14 行封顶），`/keys` 里的 `click ⛶` 一行随之删除。
  右端只留下 `↥`（跳回你上一条输入）。
- `ctrl+o` 从「展开/折叠」两态改成三态轮换：**摘要**（默认，每次调用一行）→ **预览**（每次调用一张
  卡片加尾部 4 行预览）→ **全展开**（思考与工具结果全部摊开），再回到摘要。摘要把连续的工具调用
  归成一个块，块首一行汇总——`13 次工具调用 · 13 条命令 · 1 条失败`（没有命令或没有失败时那两段
  自动省略），块里每次调用一行，前面挂树枝——`├─ 运行 ls -la ...`，最后一条收成 `└─`。已完成的调用
  不带圆点，只有还在跑的那条留一个转圈（挂在树枝后面）。一屏就能看完整轮工具调用，长会话不再被成片的输出淹掉。失败的调用以它自己的首行消息开头
  （`Failed shell request · 1 invalid field`），而不是硬套一个动词，其余行点一下才出来；成功的
  调用点一下展开它的完整输出。块首那行不响应点击。
  夹在中间的空助手消息不算断点，所以跨多步的一轮仍读作一个块；有正文的助手回复和思考照旧把它切开。
  `/keys` 里 `ctrl+o` 的说明同步改。每次调用的动词固定用英文（`Ran` / `Read` / `Wrote` / `Edited` /
  `Searched`）——那行后面跟的是命令或路径，认不出名字的工具本来就显示英文工具名；块首的计数、
  点击提示和失败消息照旧按界面语言。

## [0.1.14] - 2026-09-24

### 新增

- 任务执行中可以通过 `ask_user_question` 向用户提问；终端弹窗支持是/否、单选、多选和文字回答，提交后同一轮任务继续。按 `esc` 可取消问题。

### 修复

- 并发弹窗按到达顺序排队。屏幕上已有权限请求或任务提问时，第二个弹窗此前会把它直接顶掉：
  被顶掉的一方拿不到回答（应答通道随弹窗一起丢弃，等待中的工具或子代理只看到一次取消），
  用户也从没看见它问了什么。现在后来的弹窗排队等待，答完一个再显示下一个；轮到某个弹窗时
  提问方已经走了（比如子代理被中断），它会被跳过而不是弹出来。权限弹窗和任务提问同时在场
  时，权限画在上层并独占键盘，粘贴不会落进被盖住的那个提问；父回合结束也不再清掉仍在运行
  的子代理的提问。有些终端把按键包成粘贴发来，这类输入同样按按键处理——esc 与 ctrl+c 照常
  取消提问，而不是被写进回答。
- 图片提示真的到达模型了。贴在 composer 里的图片此前只是一枚 chip 加一条回显：进程内的
  通道只搬文本，图片字节从未离开终端，模型收到的提示里没有它。现在图片随提示走完整条链路
  ——入队、排队中编辑、Send Now 直插、子代理 steer、会话快照——以 base64 图片块按原顺序
  和文字一起出现在请求里。支持 png / jpeg / gif / webp；一条提示内联的图片合计上限
  32 MiB（超出会提示并拒绝），带图片的会话请求预算从 4 MiB 放宽到 48 MiB。
- 排队消息的落盘文件按工作区分家。队列此前写在 `$ABYLAB_HOME/queued/<会话 id>.json`：
  文件名只认会话 id（而它是用户给的），两个工作区里同名的会话共用一个文件，重启后 A 工作区
  的待发提示会出现在 B 工作区；旧路径还把 id 里非字母数字的字符一律折成 `_`，`a/b` 和
  `a_b` 撞在同一个文件上；文件里也只有文本，排队的图片一重启就没了。现在路径是
  `queued/<工作区哈希>/<转义后的会话 id>.json`，id 里除 `[A-Za-z0-9_-]` 外的字符都按字节
  无损转义成 `~XX`，记录保留文本和图片块。损坏或版本不认识的文件不再被当成空队列静默读掉：
  加载时把错误报给用户，保存时拒绝覆盖它（免得毁掉还能手工抢救的内容）；旧位置的队列文件
  会连路径一起报告，由用户确认内容后自己搬过去。写盘经过临时文件加 rename，写不进去的
  队列改动会回滚并报错，而不是假装成功。
- 安装器先验证再替换。升级时 `install.sh` 此前直接 `install` 覆盖已装好的二进制：下载被
  截断、架构不对、或在新系统上根本起不来，原来能用的 abylab 也就被覆盖掉了。现在先把新
  二进制装进目标目录里的临时文件，跑一次 `--version` 确认它能启动，才 `mv` 覆盖到目标路径；
  验证失败就删掉临时文件、保留旧版本，并以非零状态退出。

## [0.1.13] - 2026-09-23

### 新增

- `abylab --uninstall` 卸载 abylab：先列一张单子，分两半 —— 数据那半是
  `$ABYLAB_HOME` 下本程序自己保存的全部数据（`settings.json`：语言、模型、推理强度、
  权限预设、外观；`abylab-modes.json`：按工作区缓存的上次模式；`.credentials.yaml`：
  `/login` 存下的 API key；`sessions/`：会话日志；`queued/`：排队消息），程序那半是这台
  机器上的每一个 `abylab` 二进制（正在运行的这个可执行文件、`$PATH` 每个目录里的、安装器
  默认的 `~/.local/bin`，以及 `$ABYLAB_BIN_DIR`）。计划逐行列出 `remove` / `keep` 和体积、
  文件数，然后分两次确认：先问数据、再问程序 ——"保留数据、只删程序"就是第一个问题答 n；
  `--keep-data` 直接替数据问题答 n，`--yes` 两个都免问（脚本化时 `--yes` 是整清，
  `--keep-data --yes` 是只删程序）。两个问题都答 n 就什么也不动。没有单独的 `/reset`：
  清数据就是这里的第一个问题。
- 名单是白名单，不扫目录：手写的 `~/.abylab/AGENTS.md`（那是你的文件，程序只读它）和
  `--session-root` 指到 aby home 之外的会话库都会在计划里写明"保留"；home 自己永远不是
  删除目标。程序那半也只认还叫 `abylab` 的文件，符号链接连它指向的文件一起处理（跟链有
  上限，成环不会挂）；Homebrew、Nix 装的副本只列 `keep` 不动手；`~/.cargo/bin` 里的副本
  交给 `cargo uninstall abylab-tui`，`~/.cargo/.crates.toml` 的登记随之消失。shell 启动
  文件一个字节都不动 —— 安装器写进去的那行 PATH 留在原处，想清可以自己删。
- 命令行这边从不读 stdin（管道里进来的可能是别的东西），在 `/dev/tty` 上问 `[y/N]`；
  没有终端可问就报错退出，要免确认得明写 `--yes`。删除按路径报告结果，有删不掉的就以
  非零状态结束。这个开关不启动界面，其他启动选项一概忽略——只有 `--session-root` 有意义，
  它决定检查哪个会话库。要是还有别的 abylab 在跑，计划里会多一行提示：它会把自己的设置
  和会话日志写回来，先退出它才干净。

### 修复

- `ABY_TURN_TIMEOUT` 去掉了。它此前是**每个轮段的总时长**上限（默认 600 秒）：模型思考、
  流式输出、每个工具调用都算在里面。一个跑了十几分钟、每一步都在推进的回合，会在第 600 秒
  被就地打断——正在执行的工具变成"结果未核实"，已经流出来的回答丢掉；自动续跑三次之后，
  屏幕上就是 `· 回合结束 · error` 加那句
  `timed out (ABY_TURN_TIMEOUT=600, ABY_TOOL_TIMEOUT=60 seconds; ABY_AUTO_CONTINUE=3)`。
  可是回合本来就不该有这种时限：它只是本地的一个循环，DeepSeek 那边看见的是一条条无状态
  请求，"这个回合跑了多久"从来不在它的规则里，harness 的 agent loop 对单个步骤也**没有**
  任何时限。现在时间上的限制只剩 `ABY_TOOL_TIMEOUT` 一条（见下一条），而真正会卡住的地方
  各有其主：每条请求归传输层（连接 15 秒 / 首字节 120 秒 / 流空闲 60 秒），每个工具调用
  归它自己的预算，跑飞的循环归 `ABY_MAX_REQUESTS`／`ABY_MAX_TOOL_CALLS`（默认各 1000），
  用户随时可以按 Esc。
- 工具的时限不再把整个回合做掉，也不再压过工具自己的设置。`ABY_TOOL_TIMEOUT`（默认 60 秒）
  以前对每个工具调用一律生效，比工具自己声明的预算还硬：`bash` 的 `timeoutMs` 明明可以要到
  600 秒（`bash_max_timeout` 封顶），命令到 60 秒就被执行器砍掉；前台 `subagent` 等待子回合
  也一样，子代理干得久一点，等待就先被掐断。现在工具自己声明的预算说了算（harness 也是按
  工具读它自己的 `timeoutMs`，不是给所有工具套一个数），`ABY_TOOL_TIMEOUT` 只兜底那些没声明
  的工具（`read`／`write`／`edit` 这类），前台委派子代理和 `wait_agent` 干脆不计时——子回合
  自己会结束。到点先请工具停下、给它自己的清理宽限收尾，然后把结果作为一条工具错误交回模型
  （写明被中止、结果未核实），回合继续往下走——harness 的 `dsh-tool-call-timeout-policy`
  同样是回一条错误结果，而不是让回合 `error` 收场。
- 超时那行错误不再笼统：请求或工具调用到点时会说明是它自己的时限（没声明预算的工具按
  `ABY_TOOL_TIMEOUT` 秒停），并照旧列出当前环境变量值和"继续输入可续跑"。

## [0.1.12] - 2026-09-23

### 变更

- shift+tab 轮换权限时会说明自己在等：驱动按轮次串行处理命令（一轮进行中，命令通道要到这轮结束
  才被读取），所以工作中按下的切换是排在这轮之后生效，而不是失败。此前这条命令发出去就没有下文
  ——chip 要等宿主的持久回执才动，忙碌时既不提示也不变化，按下去像是坏的。现在会显示
  `权限 → 只读 · 这轮结束后生效`（首轮 prompt 之前暂存的那次仍是 `权限 → 只读 …`），并且提示里
  用 chip 上的中文预设名，不再打印 `read-only` 这样的内部 id。
- shift+tab 的步进改成以"上一次请求"为基准，而不是 chip 上的值：一轮进行中连按两次，此前第二次
  算出的目标与第一次相同，宿主把重复项按等值切换丢弃——按得越急越不动，一轮里按 N 次只前进 1 格。
  现在每次按键都前进一格，宿主按顺序应用；重复请求同一个预设（例如在 `/permission` 里又选了正在
  路上的那个）不再重复下发，免得轮末白重建一次本地工具。文案也一并改了：`/help` 与 `/keys` 原写
  `workspace-write ⇄ full access`，实际是只读 → 工作区可写 → 完全访问三个预设一轮，从默认的
  `danger-full-access` 起按第一次落在"只读"上——正是这条错的括号让人觉得切换失灵。
- 权限 chip 不再给 `完全访问` 加亮：meta 行左侧那个 chip 现在是三种预设共用一个色调
  （`fg_tertiary`），和它后面的 approval chip 一致。此前 `danger-full-access` 走 warn 色
  （琥珀），可是信任目录的会话整天就停在这个预设上——一直亮着的颜色不携带信息，反而把
  同一行里真的会变的排队计数（也用 warn）盖过去。标签照旧随预设变（只读 / 工作区可写 /
  完全访问），预设的完整说明仍写在 `/permission` 选择器和 `/status` 里。
- `--help` 不再列调优用的环境变量：末尾那段 `ENVIRONMENT (advanced tuning; no flags):`
  （`ABYLAB_HOME`、`ABY_MODEL`、`ABY_MAX_REQUESTS`、`ABY_CONTEXT_WINDOW`……）整块删掉，帮助
  止于选项表。这些变量只有高级调优会碰，日常用到的都是它们对应的选项；`$ABYLAB_HOME`、
  `$ABY_MODEL` 这类默认值仍在选项表里点名，压缩和超时那几个仍在 [TECH.md](TECH.md) 里逐个交代。
- 状态栏常驻一条排队计数：只要有排队消息，meta 行（composer 下边框）左侧就一直显示
  `· 2 条排队中`，草稿为空时再带上 `⌥↑`。此前"队列里有几条"只在三处出现，且每一处都会
  消失：时间线尾行的 `· N 条排队中` 在模型流式输出时整行被跳过（那正是排队最多的时候）、
  右下角的 `⏎ 发送队首` 只在运行中且草稿为空时出现、入队瞬间那句 tip 只有 4 秒。计数与草稿
  无关，一律显示；`⌥↑` 那一截只在队列列表真的能打开时出现（草稿为空且没有待发图片），因为
  列表要求 composer 空闲——常驻的位置不该挂一个按下去只会被拒的手势。队列既然有了常驻位置，
  右下角那句 `⏎ 发送队首` 也去掉了：这一行不再复述队列（草稿为空时按 enter 会立即送出队首，
  这条仍写在 `/keys` 里）。
- 时间线不再出现使用提示：启动只留 wordmark、项目 URL 和四项启动事实（版本 / 工作目录 /
  权限 / 模型），`/new` 开新会话也不再跟一条 `提示 · …`。七条轮换提示（esc 中断、enter 排队、
  `@` 引用文件、ctrl+t 主题……）连同推进它的下标一起删掉了 —— 按键和命令本身没变，`/keys`、
  `/help` 和斜杠菜单里照样列着它们。
- 没检测到 key 时的启动引导换了排版：一句说明，`/login sk-xxxxxxxx` 单独放进一个代码框
  （要做的只有这一件事），下面一条横线隔开"key 落在哪 / 怎么管理"。原来是 1./2./3. 三步，
  命令行混在编号里，读起来跟散文差不多。
- 退出时打印的信息排成了带横线的块：上下各一条 `─`（宽度跟着终端走，最宽 72 列），中间是
  `abylab · 会话已结束 · <会话 id>`，下面「下次继续」的 `abylab --session-id <id>` 独占一行
  （整行选中就是那条命令），末尾一行给 `/resume`。原来是三行 `标签：值` 的平铺。

## [0.1.11] - 2026-09-22

### 变更

- 删掉了 `glob` 和 `grep` 两个内置工具：工作区搜索回到 bash（`rg`/`grep`/`find`），
  请求前缀因此少约 1.6KB，也少一套要维护的搜索语义。删的理由是它们的输出契约撑不起
  日常使用：`grep` 只给"行号 + 匹配行"、默认 100 条封顶，要计数或只要文件名清单就得换
  bash；`glob` 只列文件、不列目录，也不给大小/时间（只有个 mtime 新→旧）。而实测里模型
  本来就更常走 shell——探针对照中 bash 占 16.7%，其中一半以上是搜索/分页。代价说清楚：
  在 `read-only` / `workspace-write` 预设下，从此每次搜索都要过一次审批弹窗（`read` 仍是
  唯一免审批的读工具）；`danger-full-access` 不受影响。系统提示同步改写，不再提名
  glob/grep，改为直接点名 `rg`/`grep`/`find`。

## [0.1.10] - 2026-09-22

### 变更

- 系统提示点明了工具分工，并且**点名要替代的 shell 命令**（照 harness 每个工具一段提示的
  形状）："Read files with read — not cat or sed. Search them with glob and grep — not
  shell find or rg"。此前那句提示只点了 read/write/edit/todo_write，"搜索"从未出现，
  模型于是习惯拿 bash 一把梭（实测你机器上 15 个会话 2112 次工具调用里 bash 占 78%，
  其中 781 次 `| head`、453 次 `sed -n`/`awk` 都在当分页器用，而 glob 只有 2 次、
  grep 15 次）。点名到命令而不是笼统说"别用管道"，是为了留住合法用法：裁**命令输出**
  （`cargo test | tail -20`）仍然可以，翻文件/搜文件不行。被点名的三个读工具在工作区根
  内、任何权限预设下都不弹审批，bash 在 read-only / workspace-write 下每次都要问。
  只影响新会话：`/resume` 或 `--session-id` 载入的旧会话仍用创建时存下的那句提示。

## [0.1.9] - 2026-09-21

### 变更

- ctrl+enter 现在是「插话」而不是「取消重发」：消息进运行中 agent 的收件箱，SDK 在
  下一个工具批次结束的边界把它当成一条普通 user 消息追加进历史（模型已经收尾就再
  跑一步），所以当前这一步的流式输出和已完成的工具结果都保留，也不会再打出「已中断
  —— 本轮已取消」。中断只剩 esc 一个入口。错过窗口不算失败：消息会作为下一次唤醒
  的回合投递；连运行中的回合都没有（比如还没 `/login`）时界面把那条例回客户端队列，
  回声重新带上排队底色。
- 新增 `/enter queue|steer` 忙碌态偏好：决定普通 enter 在 agent 运行时是排队还是插话，
  ctrl+enter 始终是另一种；空闲时两种手势都是直接发送。默认仍是排队，写进
  `settings.json`，重启保留。composer 的快捷键提示行、`/help`、`/keys` 都会跟着变。
- 空草稿按 ctrl+enter 现在会把队列里每一条按 FIFO 依次插话进当前轮次（原先空草稿按
  ctrl+enter 是空操作）；没有正在跑的回合时只把队首当普通提示词发出，其余留在队列。
- 插话在时间线上分段可见：按下 ctrl+enter 后气泡先标 `steering`（待插话），Agent 在
  下一个边界真的收下它（SDK 那次 drain 会回报 `SteerAdmitted`）标记才消失；被拒绝或
  错过窗口的则退回 `queued`。以前只有那句"已 steer"提示，看不出到底落没落。
- `⌥↑` 队列列表里可以逐条立即插话：ctrl+enter 把高亮那条送进正在跑的轮次（列表原地
  刷新、高亮跟到补上来的那一条，删空才关闭），和 composer 的 ctrl+enter 是同一个
  手势。列表标题和 `/help`、`/keys` 都写上了这个键。
- 排队消息能扛住重启：队列每次变动都会写
  `$ABYLAB_HOME/queued/<会话>.json`（每个会话一个文件，排空即删）。启动或 `/resume`
  到那个会话会把条目画回时间线并**先按住**——它们原本排在一个已经不存在的回合后面，
  不会替你花掉；这个进程真正跑完一轮之后按 FIFO 放出。文件缺失或损坏就当成空队列，
  行为和 `settings.json` 一致。
- 还没入轮的消息整块渲染在时间线尾部：`⏳ N 条待插话` 起一段，插话气泡排在那里，
  直到 Agent 在边界收下它们才回到自己的时间位置（harness 的 pending-steering 行）。
- 队列搬进了驱动，客户端只看快照：`CtlEvent::Queue` 每次变动都发全量行，任何客户端
  （现在的 TUI、以后的第二个进程或 Web UI）看到同一份 FIFO。行里带 placement，
  排队和插话是同一个列表里的两种状态。
- 投递顺序由驱动的空闲等待决定：先处理已经排队的命令（删除/编辑/新的插话），再清掉
  被轮次拿走的行，最后才把队首当提示词发出——所以一条"删除"永远不会输给它要改的
  那一行的边界。被拿走的 id 留墓碑，迟到的排队命令不会把已投递的行变回排队。
- 队列持久化跟着搬家：写 `$ABYLAB_HOME/queued/<会话>.json` 的是驱动；读回来时行是
  held，等这个进程真正跑完一轮才放行（旧行为在客户端，语义不变）。

### 修复

- 「已 steer —— 在 Agent 下一步生效」这句提示终于是字面意思：过去 ctrl+enter 实际
  是取消当前段再重发提示词，界面还会顺手报一次中断。
- 网站和两份 README 的按键行回到和键位表一致：立即发送写的是 ctrl+enter（旧文案
  写的 ctrl+x 其实是剪切选区），队列列表 `alt+↑`（mac `⌥↑`）也补了上去。0.1.8 里
  TUI 的提示已经改成了 ctrl+enter，漏的就是站点那两页。

## [0.1.8] - 2026-09-21

### 新增

- `⌥↑` 的排队列表现在也能删消息：除了 ↑/↓ 选择、enter 编辑，还有 `ctrl+d` 删除
  高亮的那条（同样两次确认，第一次按下时那一行会写明"再按 ctrl+d 删除"）。删完列表
  原地刷新、高亮落到补上来的那一条，可以连着删；删空就关掉弹窗。选择器标题和
  `/help`、`/keys` 都写上了这个键，弹窗宽度也会照顾标题，不再把提示裁掉。
- 退出时会打印会话 id 和回去的路：离开界面后（alternate screen 已还原）分三行输出
  `会话 id：aby-…`、`下次继续：abylab --session-id aby-…`、`或在程序里用 /resume 选择`
  ——一行一件事，恢复命令单独一行、方便整行复制。打印的是退出时屏幕上的那个会话，
  中途 `/resume` 换过会话也报新的这个；两种界面语言各用自己的措辞。

### 修复

- 编辑过的排队消息会重画它的回声：`⌥↑` 里改完一条再 enter 保存，队列里的条目换了，
  时间线上的气泡过去还是旧文案。现在保存时就地重画这组气泡（在原来的位置上，不打乱
  队列顺序），块的数量或种类变了也照重画，队列、`↥` 跳转和其余条目的下标一起重算。
- 删掉的排队消息不再留在时间线上：`⌥↑` 编辑里 `ctrl+d` 删掉一条排队消息后，它的
  回声气泡过去还挂着 `queued`（这条消息其实从没发出去）。现在删消息就是删气泡，
  队列里排在它后面的条目跟着接到新位置（`↥` 跳转、正在等待的 steer 一起重算），
  运行时退出丢掉整条队列时也照此处理。
- 工具卡片不再空着：`get_goal` 这类由 agent 自己作答的调用，界面拿到的过去是工具
  交出来的占位输出（`get_goal` 是空字符串），于是卡片只剩下一行 `get_goal {}`、下面
  没有内容。现在结果事件、检查点和下一轮请求带的都是提交后的那一条：读目标显示
  目标快照，改目标显示 `goal → …`。无参数调用也不再在工具名后面挂一个什么也没说
  的 `{}`。

## [0.1.7] - 2026-09-21

### 修复

- 会话文件不再随 checkpoint 膨胀：此前每次 checkpoint 都整份追加一遍快照，一个
  README 任务就能把日志写到 64 MB。现在一条日志只有一条头部、一条标题、一个锚点，
  之后只追加 delta：delta 只带 items/requests 分叉的那段尾巴，其余小字段整份替换，
  攒够 64 条 delta（或某条 delta 比锚点还大）就经同步的临时文件原子改写成新锚点，
  改写失败不会碰坏上一份状态。载入时把 delta 折到最后一个锚点上再校验，恢复语义和
  所有调用方签名都没变。
- 恢复和切换不再丢历史：恢复前先拿下该会话的写锁，第二个进程顶不掉正在写的会话，
  它会等锁、之后读到的是最新快照；没有 key 时发出的恢复意图留到 `/login` 之后，
  登录后读到的仍是最新历史、标题也在。切换失败时界面留在原会话——视图、草稿、队列
  都不动，等驱动确认新会话就绪才切过去，为旧会话敲的提示语不会落到新会话上。
- 工作区存储改按规范路径的 SHA-256 摘要分区：路径不同、名字相撞的工作区不再共用
  目录；旧目录核对日志里的 `cwd` 后仍可原地恢复，沿用同一把写锁。
- 会话带的配置（模型参数、系统提示）会随 checkpoint 记下来，重放后仍在；载入时
  逐条校验折进来的 delta，损坏的记录被拒，不会当成正常历史读进来。

## [0.1.6] - 2026-09-20

### 新增

- 技能（skills）：把 markdown 指令文档放进当前工作区的 `.agents/skills/`（`<名字>.md` 或
  `<名字>/SKILL.md`），就能用 `/skill` 调用。发起 `/名字 [参数]`（手打或经 `/skill`），
  Agent 把该文件正文注入本轮；正文是普通用户消息，跟着历史压缩、进快照，会话标题仍取
  你打的那行命令。技能不进 `/` 菜单：那一列只有内置命令，技能只在 `/skill` 下出现。
- `/skill <名字> [参数]` 按名字调用技能，内置命令同名（比如 `/plan`）时也能用。
  没有单独的列表命令：打一个空格，候选里就是全部技能（名字、参数提示、描述和来源
  文件），边打边过滤，Tab 只补全不发出。声明了参数提示的技能选中后等你填参数，
  不需要参数的选中即发出；不带参数时弹出同一份清单，工作区里没有技能就直接给出
  `.agents/skills/` 的位置。
- 模型侧多了 `skill` 工具：请求前缀里带一份技能索引，模型可以按需读某个技能的正文，
  不带参数时列出全部。单个工作区最多 128 个技能；技能只从工作区的 `.agents/skills/`
  读（不读 aby 主目录），启动时扫一次，改了文件要重开会话。
- 技能正文上限 64 KiB（和 AGENTS.md 基线的预算同一个数）。超出不再跳过，而是按字符
  边界截断并在末尾追加一行 `[Truncated: …]`，同时在界面上报一次 warning：技能照旧
  可用，被切掉的部分作者和模型都看得见。

## [0.1.5] - 2026-09-20

### 新增

- 启动画面在 ASCII wordmark 和项目地址下面多出四行启动信息：版本号、工作目录、
  权限预设、模型（设了 effort 就带在模型那行后面）。标签跟随界面语言，值不变。
- 底栏的 `↓ N`（N = 离底部多少行）现在是按钮：点一下翻回最新的行、重新跟随尾部；
  指针停上去会变亮。往上翻过对话时它才出现。
- `/resume` 在任务运行中也立刻列出会话：这条列举是纯读操作，走驱动的查询侧通道，
  不再排在正在跑的回合后面；选中后真正载入仍等这一轮结束，提示里会写明
  （「（本轮结束后）」）。`/model` 的实时模型列表走同一条通道，回合中也能立刻
  补全（`/login` 换过 key 也照样用新 key）。

### 修复

- 任务进行中执行 `/resume` 会一直等待：会话列表原先排在驱动那条单队列里，
  一个回合跑完才轮到它，选择器因此不弹出。同一队列也让 `/model` 的实时模型
  列表等到回合结束才到。

### 变更

- README 中英文两版都只留「快速开始」一节（装、取 key、`/login`、选项、环境变量、
  `settings.json`）：极简/单文件/快、命令列表、按键、轮次预算、技术说明、许可证各节
  删掉 —— 命令、按键、特性和 crate 表本来就在 abylab.ai 上，内部细节留在 TECH 两份。
  指向这些旧小节与锚点的交叉引用改到 TECH 或站点。
- 快速开始里也不再讲安装机制：预编译平台矩阵、`SHA256SUMS` 校验、装到哪、PATH
  怎么写、musl 静态链接、源码构建全部删掉，只留一条安装命令（这些选项在站点那行
  提示和 `install.sh --help` 里）。
- 补齐中英文对照：中文 README 补上选项表、环境变量、
  `settings.json`；英文 README 补上「可以让 AI 帮你安装」一句；英文 TECH 补上只有
  中文版才有的时限段。站点两版页面核对为同构。
- 界面文案（客户端自有 chrome）全部跟 `/lang` 走：时间线通知与工具卡尾注、
  子代理与 Agent 侧栏、计划 chip、权限预设的含义与 `· 当前`/`· 默认` 标记、
  斜杠参数提示、命令反馈与状态行、附件预览浮层、审批弹窗标题、终端过小提示。

## [0.1.4] - 2026-09-20

### 变更

- Linux 只发一种包：每个架构一个 musl 静态二进制，不再有 glibc 版本，
  没有 glibc 下限要长期维护；二进制在 Alpine 容器里按架构原生构建。
- 输入框多两行、底栏更安静：随时读得清模式和模型，又不会挤掉草稿。

### 修复

- `scripts/check-glibc-floor.sh` 的 objdump 兜底分支会如实回答解释器问题，
  静态产物不再被误判。

## [0.1.3] - 2026-09-20

### 新增

- 启动画面（ASCII wordmark + 项目地址）。
- `/status`、`/help`、`/keys` 是浮在对话上的弹窗，不写进会话记录。

### 变更

- 输入框周围的框架更安静：模式、模型和状态不再抢草稿的注意力。
- Linux 二进制改在 bullseye 上构建，并用 `scripts/check-glibc-floor.sh`
  断言 glibc 下限；发布流水线把 tarball 镜像到 abylab.ai/downloads，
  安装脚本优先走镜像。
- `https://abylab.ai/install.sh` 直接由站点提供（不再跳到 GitHub）。

## [0.1.2] - 2026-09-20

### 新增

- `@` 文件浏览器：Tab 落在目录上会进去，输入即过滤，跳过 `.git`、`node_modules`
  等重目录。
- 输入框上沿的 `todo_write` 任务清单与进度 chip，点击打开完整清单。
- 提示行里的项目路径带上 `:分支`（直接读 `.git/HEAD`，不启动 git 进程）。
- 站点 abylab.ai 上线（Pico CSS + Cloudflare Pages），文档拆成 README 与 TECH
  的中英两版。

### 变更

- 全新安装默认落在 One 主题包的深色外观。
- 安装脚本把安装目录写进 shell 启动文件（`--no-path` 只打印），并引导用户用
  `/login` 存 key。

### 修复

- 因超时中断的回合可以续跑：未结算的工具调用标记为「未核实」，下一条消息接着跑
  同一个回合。

## [0.1.1] - 2026-09-19

### 修复

- 发布任务的 checkout 缺失导致 publish 从未真正执行。
- README 的措辞与排版。

## [0.1.0] - 2026-09-19

首个版本：abycore agent SDK、abylab-backend 驱动、abylab-tui 画布（composer
卡片、markdown、主题包、`@` 文件提及、图片缩略图），一条命令的安装脚本和
release 流水线。

[Unreleased]: https://github.com/zhang0098/abylab/compare/v0.1.17...HEAD
[0.1.17]: https://github.com/zhang0098/abylab/compare/v0.1.16...v0.1.17
[0.1.16]: https://github.com/zhang0098/abylab/compare/v0.1.15...v0.1.16
[0.1.15]: https://github.com/zhang0098/abylab/compare/v0.1.14...v0.1.15
[0.1.14]: https://github.com/zhang0098/abylab/compare/v0.1.13...v0.1.14
[0.1.13]: https://github.com/zhang0098/abylab/compare/v0.1.12...v0.1.13
[0.1.12]: https://github.com/zhang0098/abylab/compare/v0.1.11...v0.1.12
[0.1.11]: https://github.com/zhang0098/abylab/compare/v0.1.10...v0.1.11
[0.1.10]: https://github.com/zhang0098/abylab/compare/v0.1.9...v0.1.10
[0.1.9]: https://github.com/zhang0098/abylab/compare/v0.1.8...v0.1.9
[0.1.8]: https://github.com/zhang0098/abylab/compare/v0.1.7...v0.1.8
[0.1.7]: https://github.com/zhang0098/abylab/compare/v0.1.6...v0.1.7
[0.1.6]: https://github.com/zhang0098/abylab/compare/v0.1.5...v0.1.6
[0.1.5]: https://github.com/zhang0098/abylab/compare/v0.1.4...v0.1.5
[0.1.4]: https://github.com/zhang0098/abylab/compare/v0.1.3...v0.1.4
[0.1.3]: https://github.com/zhang0098/abylab/compare/v0.1.2...v0.1.3
[0.1.2]: https://github.com/zhang0098/abylab/compare/v0.1.1...v0.1.2
[0.1.1]: https://github.com/zhang0098/abylab/compare/v0.1.0...v0.1.1
[0.1.0]: https://github.com/zhang0098/abylab/releases/tag/v0.1.0
